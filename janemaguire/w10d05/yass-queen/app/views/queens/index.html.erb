<p id="notice"><%= notice %></p>

<h1>Queens</h1>
<p class="intro">Explore drag acts from all over the world and find out where you can see them perform. Are we missing a great act? Let us know! <%= link_to 'Add a new queen', new_queen_path %></p>

<table>
  <thead>
    <tr>
      <th></th>
      <th>Queen</th>
      <th>Quote</th>
      <th>Venues</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @queens.each do |queen| %>
      <tr>
        <td><%= image_tag queen.image, width: 200 %></td>
        <td>
          <h3><%= queen.name %></h3>
          <p>Nationality: <%= queen.nationality %>
            Hometown: <%= queen.hometown %></p>
          <p><a href="<%= queen.website %>">Website</a></p>
        </td>
        <td><%= queen.catchphrases %></td>

        <td>
          <ul>
            <% queen.venues.each do |venue| %>
              <%= venue.name %>
            <% end %>
          </ul>
        </td>
        <td><%= link_to 'Find out more', queen %></td>
        <td><%= link_to 'Edit queen', edit_queen_path(queen) %></td>
        <td><%= link_to 'Delete', queen, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
