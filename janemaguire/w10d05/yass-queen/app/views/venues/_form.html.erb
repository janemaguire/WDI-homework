<%= form_for(venue) do |f| %>
  <% if venue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(venue.errors.count, "error") %> prohibited this venue from being saved:</h2>

      <ul>
      <% venue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<container class="form">
    <p class="form-label"><%= f.label :name %></p>
    <p class="form-field"><%= f.text_field :name %></p>

    <p class="label"><%= f.label :image %></p>
    <p class="field"><%= f.text_field :image %></p>

    <p class="label"><%= f.label :city %></p>
    <p class="field"><%= f.text_field :city %></p>

    <p class="label"><%= f.label :country %></p>
    <p class="field"><%= f.text_field :country %></p>

    <p class="label"><%= f.label :opened %></p>
    <p class="field"><%= f.text_field :opened %></p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</container>
